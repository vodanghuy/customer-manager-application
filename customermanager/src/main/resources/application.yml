spring:
  datasource:
    url: jdbc:mysql://localhost:3306/customermanager
    username: root
    password: iamdaghuy
  jpa:
    database-platform: org.hibernate.dialect.MySQL8InnoDBDialect
    generate-ddl: true
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        globally_quoted_identifiers: true
        dialect: org.hibernate.dialect.MySQL8Dialect
        format-sql: true
  sql:
    init:
      mode: never